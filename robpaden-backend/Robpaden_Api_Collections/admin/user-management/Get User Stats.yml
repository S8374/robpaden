info:
  name: Get User Stats
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/users/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
