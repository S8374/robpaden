info:
  name: Get Office Stats
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/offices/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
