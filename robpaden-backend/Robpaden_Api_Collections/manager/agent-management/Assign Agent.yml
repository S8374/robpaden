info:
  name: Assign Agent
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/manager/agents/123/assign"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
