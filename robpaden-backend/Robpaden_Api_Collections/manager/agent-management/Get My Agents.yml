info:
  name: Get My Agents
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/manager/agents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
