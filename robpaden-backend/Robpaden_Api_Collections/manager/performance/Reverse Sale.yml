info:
  name: Reverse Sale
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/manager/performance/audit/1/reverse"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
