info:
  name: Get Performance History
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/manager/performance/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
