info:
  name: Get Agent Performance
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/manager/performance/agent/42"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
