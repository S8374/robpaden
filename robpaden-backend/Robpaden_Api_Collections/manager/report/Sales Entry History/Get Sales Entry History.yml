info:
  name: Get Sales Entry History
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/manager/reports/sales-entry-history?range=today"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
