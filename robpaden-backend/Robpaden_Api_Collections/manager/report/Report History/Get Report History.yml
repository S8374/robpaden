info:
  name: Get Report History
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/manager/reports/history?range=this-month"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
