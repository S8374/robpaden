info:
  name: Get Report Summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/manager/reports/summary?range=today"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
