info:
  name: Get Recipients
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/manager/reports/recipients"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
