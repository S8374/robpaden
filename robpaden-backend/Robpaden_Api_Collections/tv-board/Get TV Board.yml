info:
  name: Get TV Board
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/tv/board?password=your_tv_password"

headers:
  x-device-id: example_device_id
  x-device-name: Chrome Browser

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
