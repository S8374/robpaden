info:
  name: Edit Sale
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/manager/performance/audit/1/edit"
  body:
    type: json
    data: |-
      {
        "newCount": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
