info:
  name: Get Agent Today Audit
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/manager/performance/agent/2/audit-today"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
