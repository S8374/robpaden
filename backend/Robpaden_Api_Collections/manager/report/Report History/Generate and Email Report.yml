info:
  name: Generate and Email Report
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/manager/reports/generate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
