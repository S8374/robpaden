info:
  name: Get My Teams
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/manager/teams"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
