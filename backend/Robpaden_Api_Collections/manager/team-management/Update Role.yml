info:
  name: Update Team Member
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/manager/teams/1/members/42"
  auth: inherit

body:
  type: json
  data: |-
    {
      "name": "Updated Name",
      "isActive": true,
      "dailyGoal": 10,
      "teamRole": "LEADER"
    }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
