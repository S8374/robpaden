info:
  name: Get TV Devices
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/offices/1/tv-devices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
