info:
  name: Delete TV Device
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/admin/v1/offices/tv-devices/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
