info:
  name: Get Offices
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/offices/"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3030

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
