info:
  name: Mark As Read
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/notifications/1/read"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
