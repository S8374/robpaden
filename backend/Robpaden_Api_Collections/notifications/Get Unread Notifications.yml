info:
  name: Get Unread Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
